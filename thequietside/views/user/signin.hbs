<div class="row">
    <div class="col-md-4 col-md-offset-4">
        <h1>Sign In</h1>
        {{# if hasErrors }}
            <div class= "alert alert-danger">
                {{# each messages}}
                    <p>{{this}}</p>
                {{/each}}
            </div>
        {{/if}}
        <form action="/user/signin" method="post">
            <div class= "form-group">
                <label for="email">Email</label>
                <input type="text" id="email" name="email" class="form-control">
            </div>
            <div class= "form-group">
                <label for="password">Password</label>
                <input type="text" id="password" name="password" class="form-control">
            </div>
            <!--Use of CSRF protection in order to protect our session from getting stolen or if it is stolen, new users cant be made-->
            <!--Hidden input field which gets submitted. This sends the session token which allows the server to identify if the browser sending this token is the same browser which started the session in the first place. This protect the session from getting stolen-->
            <input type="hidden" name="_csrf" value="{{csrfToken}}">

            <button type="submit" class="btn-btn-primary">Sign In</button>
        </form>
        <p>Don't have an account? <a href="/user/signup"> Sign Up!</a></p>
    </div>
</div>